# sallyport 0.1.0-alpha.1

- Binds npm's verified provenance certificate to the pinned sallyport workflow.
- Authenticates sealed bytes before and after both smoke jobs.
- Disables install-time lifecycle scripts in both smoke jobs.
- Preserves the exact signed tag object through GitHub Release writes.
- Enforces semantic release fields and three-way checkpoint SHA agreement.
- Audits rulesets, npm trust, MFA, environments, and immutable Releases.
